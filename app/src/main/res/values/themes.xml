<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Use Material3 DayNight theme; don't reference colorPrimary attr directly -->
    <style name="Theme.Boards" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
    </style>
</resources>